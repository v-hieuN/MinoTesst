# Minio Host
spring.minio.url=http://192.168.88.210:9000
# Minio Bucket name for your application
spring.minio.bucket=albums
# Minio access key (login)
spring.minio.access-key=y1GpYYy4VzZp5pd50enPgiJ4fYf53EDC
# Minio secret key (password)
spring.minio.secret-key=GbrWx03DzgciizNGdW0OEKqLH1jpQtHo

## MULTIPART (MultipartProperties)
# Enable multipart uploads
spring.servlet.multipart.enabled=true
# Threshold after which files are written to disk.
spring.servlet.multipart.file-size-threshold=2KB
# Max file size.
spring.servlet.multipart.max-file-size=5MB
# Max Request Size
spring.servlet.multipart.max-request-size=10MB

management.endpoints.web.exposure.include=*
management.endpoint.health.show-details=always
